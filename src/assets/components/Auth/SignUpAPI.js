// send username, email and password to server

// server will add an authentication key

// then send complete request to wordpress endpoint

// wordpress send back success or error message

//server script will tre to authenticate by sending username and password to another endpoint

// if success, it will return a JWT token
